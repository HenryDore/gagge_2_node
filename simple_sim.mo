model simple_room
  PIR_gagge_smoosh aline_sim annotation(
    Placement(transformation(origin = {-10, 10}, extent = {{-10, -10}, {10, 10}})));
equation

  annotation(
    uses(Modelica(version = "4.0.0")));
end simple_room;
